<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mirror_id %><br>
    <%= f.text_field :mirror_id %>
  </div>
  <div class="field">
    <%= f.label :pass_id %><br>
    <%= f.number_field :pass_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
